# ==================== PostgreSQL (RDS via SSH tunnel) ====================
spring.datasource.url=jdbc:postgresql://database-1.czgsmga2siiz.ap-south-1.rds.amazonaws.com:5432/securevaultdb?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=vaultkey2419
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Connection Pool Settings
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000

# ==================== JPA/Hibernate Configuration ====================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ==================== Flyway Configuration ====================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ==================== Redis Configuration ====================
spring.data.redis.host=redis
spring.data.redis.port=6379
spring.cache.type=redis

# ==================== MongoDB (Atlas) Configuration ====================
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://flaman:FlutterDev%402419@securevaultai.mx0qhvx.mongodb.net?retryWrites=true&w=majority}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:securevaultdb}
spring.data.mongodb.auto-index-creation=true

# ==================== Application Configuration ====================
server.port=4000
spring.profiles.active=docker
