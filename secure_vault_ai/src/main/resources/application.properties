spring.application.name=demo

# ==================== PostgreSQL Configuration ====================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/securevault_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:amansharma}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:aman2319}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==================== JPA/Hibernate Configuration ====================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ==================== Flyway Configuration for PostgreSQL ====================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baselineOnMigrate=true

# ==================== MongoDB Configuration ====================
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://flaman:FlutterDev@2419@securevaultai.mx0qhvx.mongodb.net?retryWrites=true&w=majority}
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:secure_vault}
spring.data.mongodb.auto-index-creation=true

# ==================== Redis Configuration ====================
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.cache.type=redis

# ==================== Application Configuration ====================
server.port=4000
